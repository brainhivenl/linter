# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json

id: rust/no_unsafe
message: Unsafe code is forbidden in our codebase
severity: error # error, warning, info, hint
language: Rust
rule:
  pattern: unsafe